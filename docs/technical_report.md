# 「朝夕」· 主动式 AI 生活管家 — 技术报告

> 2026 首届 openvela AI 硬件开发者大赛 · 参赛作品技术报告
> 仓库：contest2026_416_dianzinongmingong（电子农民工）

---

## 1. 信息表

| 项目 | 内容 |
|------|------|
| 作品名称 | 朝夕 · 主动式 AI 生活管家 |
| 队伍名称 | 电子农民工 |
| 团队分工 | **yuk1-r**（前期奠基）：openvela 工作区搭建与 BES2800BP 开发板 bring-up；NTC 断言崩溃的二进制补丁定位与 AP-only 烧录链路打通；zhaoxi_ui LVGL 桌面应用初版；AI Agent 固件集成（defconfig 增量、rcS 启动切换）；WiFi 联调与 TLS 网络对时补丁（HTTP Date 方案首创）；v1–v5 固件迭代；前期 Codex CLI AI 辅助开发（10 段会话日志）。**lishuangfan13-afk**（后期主力，32 个提交）：构建环境恢复与 WSL 工具链修复；三级时钟策略完善与 /data LittleFS 持久化（tmpfs 自动回退）；成果规范化入仓（manifest 映射、6 补丁系列化）；UI 真实数据联动（任务计数/WiFi 状态绑定设备状态）；三屏 tileview 与触摸键盘；圆屏内切圆安全区布局与离机几何审计闸门；CJK 字体子集流水线（262 字 + 覆盖率构建闸门）；串口资产部署脚本（回读校验 + 自动重试）；cron `daily` 定点调度与 NET_STATUS 两个框架补丁；安全治理（密钥清除）；作品文档与 6 个开发经验 Skill 沉淀；后期 OpenCode AI 辅助开发（24 段会话日志） |
| 选题方向 | AI 硬件产品创新 |

---

## 2. 摘要

「朝夕」是运行在 BES2800BP 开发板（openvela/NuttX，454×454 AMOLED 圆屏）上的**主动式 AI 生活管家**。区别于"你不问、它不说"的被动式语音助手，朝夕以 cron 定时引擎为骨架主动推送每日简报、健康提醒与待办提醒，用户以自然语言让它记事、设提醒、查天气。技术上：基于官方 `ai_agent` 框架构建"人格 + 记忆 + 技能"体系（4 个自定义设备端 Skill + SOUL/USER/MEMORY 三件套）；UI 与 Agent 双任务以文件解耦，避免嵌入式跨任务 IPC 脆弱性；为框架贡献 6 个补丁（HTTP Date 对时、/data 挂载回退、`daily` 定点调度、网络状态文件等）；自研中文字体子集流水线（262 字、覆盖率构建闸门）与串口资产部署工具（逐字节回读校验）。固件 2.33MB，真机 11 项功能实测通过；全程 AI 辅助开发（34 段会话日志可稽核），沉淀 6 个可复用开发经验 Skill。

---

## 3. 正文

### 3.1 绪论

**项目背景与问题定义**

市面上的智能音箱、语音助手绝大多数是**被动式**的：用户不发起，设备永远沉默。但在真实生活里，都市白领的痛点往往不是"查询"，而是**在合适的时机被提醒、被照顾**：

- 早晨想知道天气、待办和新闻，但不想每天主动去问一遍；
- 久坐、忘记喝水这类健康习惯，需要的是**到点提醒**，而不是事后查询；
- 临时想起的事，希望随口记下，之后被**主动**提醒。

目标场景：桌面/床头常亮的"AI 生活管家"——机器主动找事做，人只需要回应。

**技术难点**

1. **"主动"在嵌入式上的工程化**：定时触发（尤其"每天早 8 点"这类定点调度）超出了 ai_agent 框架原生 cron 的表达能力；触发的提醒如何让用户在屏幕上**看得见**（框架原生 `channel:"system"` 只写 syslog）。
2. **无 RTC 设备的网络时间**：系统时钟停在 1970 导致所有 TLS 握手失败，云端 LLM 不可达。
3. **中文显示**：LVGL 内置字体不含 CJK，全量中文字体数 MB 而固件仅 2.33MB。
4. **开发板硬件缺陷**：BES2800BP 无 NTC 热敏电阻，固件启动即触发断言崩溃循环；/data 无分区，资产重启即丢。
5. **资产部署链路**：设备端 Agent 的人格/记忆/Skill 文件需要一条不依赖网络的部署通道，且框架自带方式（`install_skill` 仅支持 https URL、`memory_write` 分词缺陷）不可用。
6. **圆形屏幕的安全区布局**：RM69330 是 454×454 **圆形** AMOLED，按 `SCREEN_W` 铺满的全宽矩形（状态栏/导航栏/信息卡/键盘）四角会被弧形边框物理裁切。难点在于：其一，嵌入式侧无 libm，几何约束求解只能用整数运算；其二，布局错误**只有烧录上板后才暴露**，一轮"编译 11 分钟 + 烧录 2 分钟"的反馈周期让试错成本极高。

**创新点**

- **产品形态**：把"主动干活"做成产品的第一性——cron 引擎 + LLM + Skill 的组合让设备不等指令就提供服务；
- **架构**：UI 与 Agent 双任务**文件解耦**（Agent 写约定文件、UI 轮询显示），规避嵌入式跨任务 socket/IPC 的堆竞争风险，每个数据源可独立调试、天然支持离线降级；
- **零框架改动的可见性打通**：复用 cron 的 `action` 机制 + 已注册的 `write_file` 工具，把提醒落到 UI 可读文件，未动框架一行就解决了"用户看不见"；
- **构建期可判定的圆屏布局**：把"元素是否被弧形边框裁切"从上板后才能发现的目视问题，变成主机侧秒级可判定的**离机几何审计**——审计脚本镜像 C 侧 `isqrt` / `circle_max_w` 逻辑，对每个元素的四角做圆内判定，与字体覆盖率闸门同属"把运行期问题前移到构建期"的思路；
- **工程化沉淀**：把踩坑经验固化为 6 个可复用开发 Skill 与 4 个设备端 Skill，形成方法论资产。

### 3.2 系统方案设计

**系统总体架构**

```text
┌─────────────────────────────────────────────────────────────┐
│                      BES2800BP（AP 核）                      │
│                                                              │
│  ┌──────────────────┐        ┌────────────────────────┐    │
│  │  zhaoxi_ui       │ 读文件  │   ai_agent 框架         │    │
│  │  (LVGL 桌面 UI)   │◄───────│                        │    │
│  │  · 首页 tile     │        │  ┌──────────────────┐  │    │
│  │  · 对话 tile     │        │  │  cron_service    │  │    │
│  │  · 设置 tile     │        │  │  (定时引擎)       │  │    │
│  │  · 触摸键盘       │        │  └────────┬─────────┘  │    │
│  └────────┬─────────┘        │           │ action      │    │
│           │ 串口 DIAG        │  ┌────────▼─────────┐  │    │
│           ▼                  │  │  Tool Registry   │  │    │
│      自诊断输出              │  │  (36 内置工具)    │  │    │
│                               │  └────────┬─────────┘  │    │
│                               │  ┌────────▼─────────┐  │    │
│                               │  │  LLM Router      │  │    │
│                               │  │  (MiMo v2.5 Pro) │  │    │
│                               │  └────────┬─────────┘  │    │
│                               │  ┌────────▼─────────┐  │    │
│                               │  │  Skill Loader     │  │    │
│                               │  │  (4 自定义 Skill) │  │    │
│                               │  └──────────────────┘  │    │
│                               └───────────┬────────────┘    │
└───────────────────────────────────────────┼─────────────────┘
                                            │ HTTPS (TLS)
                                   ┌────────▼─────────┐
                                   │  小米 MiMo 云端    │
                                   │  v2.5 Pro 推理    │
                                   └──────────────────┘
        /data（LittleFS 优先，tmpfs 回退）
        ├── config/SOUL.md, USER.md      ← 人格与用户画像
        ├── memory/MEMORY.md             ← 长期记忆
        ├── skills/*.md                  ← 4 个自定义 Skill
        ├── cron.json                    ← 定时任务定义
        ├── WEATHER.md / REMINDER.md     ← UI 天气/提醒数据源
        ├── NET_STATUS / TASKS.md        ← UI 网络/待办数据源
        └── config/config.json           ← 运行期配置（含 API Key）
```

**数据流**（以"每日简报"为例）：

```text
cron_service 定时触发 → action=write_file 写 REMINDER.md（提醒可见）
用户回应"今日简报" → LLM 按需 read_file 读取 daily-briefing.md 全文
→ 按步骤调用 get_weather / write_file(WEATHER.md) / read_file(TASKS.md)
→ 生成简报回复 → UI 2 秒周期轮询刷新天气卡片与待办计数
```

**方案论证与选型**

| 决策 | 选择 | 理由（含备选对比） |
|------|------|--------------------|
| 开发板 | BES2800BP_EVB | 大赛官方支持平台，best1700_ep 生态成熟，自带 454×454 AMOLED 圆屏 + 触摸 + WiFi，满足"桌面常亮管家"形态；备选手表类板型屏幕过小，不适合桌面场景 |
| 端/云职责 | 端侧调度与 UI + 云端推理 | 端侧负责 cron 触发、文件状态、UI 渲染（实时性要求）；LLM 推理放云端（MiMo v2.5 Pro，板端算力不足以跑可用质量的 LLM） |
| UI↔Agent 通信 | 文件解耦 | 备选 socket/IPC：嵌入式双任务共享堆易竞争（实测曾因 socket 查询导致堆损坏回退为文件方案）；文件方案天然支持离线降级（文件缺失时 UI 显示占位而非崩溃） |
| 断网/弱网降级 | 分层降级 | 天气/新闻不可用时 UI 保留上次数据或占位显示；对时失败回退编译期时间戳；/data 分区不可用时自动回退 tmpfs——**每一层降级都不阻塞启动** |
| 中文显示 | 字体子集 | 备选全量字体（数 MB，超出固件预算）；子集 262 字 + 构建闸门，改文案缺字在本地即报错 |

**关键模块设计**

- **cron_service（定时引擎）**：三类调度——`every`（间隔）、`at`（一次性）、`daily`（每日定点，本项目为框架新增）；任务通过 `action` 调用已注册工具产生**用户可见**的副作用。
- **Tool Registry**：36 个内置工具（网络、文件、系统、搜索等），自定义 Skill 以 Markdown 编写、按需加载。
- **Skill Loader**：把 Skill 的标题+摘要注入 system prompt，Agent 用 `read_file` 按需读全文——**渐进披露**，节省上下文。
- **zhaoxi_ui**：三屏 tileview（首页/对话/设置），LVGL 深色主题，自注册触摸输入；全部元素按圆屏内切圆安全区居中布局（见 3.3 机制 6）。

### 3.3 核心算法与技术原理

**AI 算法实现（云端模型）**

- 推理后端：小米 **MiMo v2.5 Pro**（`api.xiaomimimo.com`，OpenAI 兼容接口），设备端通过 `set_llm` 配置端点与密钥；密钥仅存于设备 `/data/config/config.json`（0600 权限），**不进源码仓库**（曾发生明文密钥入仓，已专项清理并建立红线）。
- Agent 模式：官方 `ai_agent` 框架的 ReAct 循环——LLM 推理 → 工具调用 → 结果反馈 → 迭代，直到产出最终回复。
- 端侧无常驻模型：端侧算力用于 LVGL 渲染与调度，推理全部上云，符合"轻端重云"的 openvela 设计哲学。

**关键机制设计**

1. **`daily` 定点调度算法**（补丁 0005）：避免 NuttX romfs 时区问题（框架既有约定不使用 `localtime_r`/`mktime`），以 `gmtime_r` + 固定 UTC+8 偏移计算"当前时刻到下一个 hh:mm 的秒差"，转换为 `every` 语义执行，与既有 `tool_get_time.c` 实现保持一致。
2. **三级时钟策略**（补丁 0001）：HTTPS `Date` 响应头解析对时（首选）→ 编译期 `__DATE__/__TIME__` 回退 → 保守时间戳兜底。解决无 RTC 设备的 TLS 证书时间校验死锁。
3. **文件解耦的状态总线**：Agent 侧把天气/提醒/网络/待办写入约定格式文件（单行紧凑、无 markdown 标题——UI 只读第一个非空行），UI 2 秒轮询。写入格式约束写进 Skill 的执行步骤，保证 LLM 产出可被 UI 解析。
4. **中文字体子集流水线**：从 UI 源码 + 运行时文案自动提取所需汉字 → `lv_font_conv` 生成 4 尺寸子集 → 覆盖率校验作构建闸门，把"缺字"从烧录后才发现的运行期问题变为本地秒级报错的构建期错误。
5. **NTC 断言二进制补丁**（工具 `ntc_patch.py`）：符号级定位（从 ELF 解析 `pmu_open` 符号计算偏移，不硬编码）+ 原指令硬校验（确认 `0xb955` 后才改写为跳转 `0xe011`，布局漂移即拒绝执行）。
6. **圆屏内切圆安全区**：以屏幕内切圆（圆心 227,227，半径 227）为约束边界，`circle_max_w(cy, h)` 由元素垂直中心与高度反解该处允许的最大宽度（`sqrt(R² - dy²) - SAFE_MARGIN`，取偶），`circle_fit_w()` 再与期望宽度取小。开方用自实现的 `isqrt_i()` 整数迭代，不引入 libm。状态栏与导航栏因此从全宽条改为居中浮动胶囊，tileview 铺满整屏、导航浮于其上；配合主机侧离机几何审计（四角圆内判定），布局回归无需上板即可拦截。

**openvela 系统能力的深度运用**

- **图形**：LVGL + NuttX framebuffer/输入驱动（`/dev/fb0`、`/dev/input0`），DSI 圆屏 + TMA525C 触摸，三屏 tileview 交互 ✓
- **AI**：深度定制官方 `ai_agent` 框架——Skill 体系、cron 引擎、人格/记忆三件套，并向框架回馈 6 个补丁 ✓
- 多媒体（语音）未落地，列为下一阶段 ASR/TTS 路线（见 3.7）
- 对 openvela 的优化/拓展：`packages/ai_agent` 4 个补丁（0001/0002/0005/0006）+ `vendor/bes` 2 个补丁（0003/0004），均为标准 `git format-patch`，可 `git am` 一键复现

### 3.4 系统实现

**软件/固件架构**

- 工作区：openvela 全量源码（repo manifest，264 仓），比赛仓以 `<linkfile>` 映射进构建树（`packages/zhaoxi_ui`）
- 应用层：`zhaoxi_ui`（LVGL 桌面，三屏 + 触摸键盘 + DIAG 自诊断）
- 框架层：`ai_agent`（含 6 个补丁中的 4 个框架补丁）
- 板级：`vendor/bes` defconfig 增量（`CONFIG_EXAMPLES_AI_AGENT_VELA`、`CONFIG_FS_LITTLEFS`、DNS、`CONFIG_EXAMPLES_ZHAOXI_UI` 等）+ `rcS.ap` 启动 `zhaoxi_ui`
- 设备端资产：8 个文件（SOUL/USER/MEMORY/cron.json/4 Skill），串口部署

**硬件设计与适配**

- 开发板：BES2800BP_ZE7/JE6_EVB_V1.2（best1700_ep 平台），454×454 RM69330 AMOLED（DSI），TMA525C 触摸，串口 COM20 @ 921600
- **是否完成全新硬件平台适配**：否（采用大赛官方支持板型）；但完成了大量**板级 bring-up 与驱动级问题定位**：
  - NTC 缺失导致的启动断言崩溃——定位到 `pmu_open+0x1a78` 的 `cbnz r5` 指令级根因，符号级二进制补丁解决；
  - 烧录链路打通——AP-only 烧录红线（禁 `nuttx_ota.bin`/`nuttx_bl.bin`）、三种下载模式进入方法、COM 口抢占规避；
  - 触摸输入链路——对比 lvgldemo（libuv 事件循环）与自研应用（轮询）的驱动注册差异，定位 `lv_timer_handler` 轮询下输入无响应的根因，改为 `lv_nuttx_init` 自注册输入设备。

**应用/交互端设计**

- 首页：状态栏（WiFi 实时状态 + AI Ready）、大时钟、问候语、天气卡片（数据源 `WEATHER.md`）、待办计数（`TASKS.md`）
- 对话页：内置触摸键盘 + 文本框（不依赖麦克风获得独立输入路径）
- 设置页：网络状态、待办统计、运行时长
- 圆屏适配：三屏元素统一按内切圆安全区居中，状态栏与导航栏为浮动胶囊；长文本（天气描述、网络状态）统一 `LV_LABEL_LONG_DOT` 省略并限定宽度，避免溢出卡片或被弧形边框裁切
- 串口 DIAG 自诊断层（`READY`/`FILE`/`TOUCH`/`NAV`/`ALIVE` 五类标签），一次烧录即可验证启动完整性、触摸链路、屏幕切换、文件数据源与心跳

**自定义 Skill（运行时，部署于 `/data/agent/skills/`）**

| Skill | 触发场景 | 职责 |
|-------|----------|------|
| `daily-briefing.md` | 用户问"今天有什么安排/早安/今日简报"，或收到 cron 推送后回复 | 查天气→写 `WEATHER.md`→读待办→查新闻→汇总输出 |
| `note-taker.md` | 用户"记一下/提醒我" | 记事写入长期记忆 |
| `reminder.md` | 自然语言设提醒 | 解析时间语义，调用 `cron_add` 建定时任务 |
| `health-reminder.md` | cron 触发久坐/饮水/用眼提醒 | 提醒话术与时段判断 |

cron 定时任务（`cron.json`，4 个）：`brief`（每 24h 简报引导）、`sedent`（每 90min 久坐）、`water`（每 2h 饮水）、`wake`（**daily 08:00** 早安问候，依赖补丁 0005）。

### 3.5 系统测试与结果分析

**测试环境**

| 项 | 配置 |
|----|------|
| 硬件 | BES2800BP_ZE7/JE6_EVB_V1.2，454×454 **圆形** AMOLED（RM69330，DSI）+ TMA525C 电容触摸 |
| 固件 | nuttx_ap.bin 2.33MB（2,445,464 字节，含 NTC 补丁，`0x47bc` 处 `0xb955`→`0xe011`） |
| 构建 | WSL Ubuntu-22.04 + openvela prebuilts（ARM GCC / CMake / Ninja） |
| 串口 | COM3 @ 921600（FTDI，验证与部署通道；早期验证轮次为 COM20） |
| 云端 | MiMo v2.5 Pro（api.xiaomimimo.com） |

**功能测试（真机实测，串口证据可复核）**

| 测试项 | 结果 | 证据 |
|--------|------|------|
| 中文显示无豆腐块 | ✅ | 屏幕目视 + 字体覆盖率检查 PASS（262 字子集） |
| 触摸输入 | ✅ | 串口 `DIAG TOUCH x=.. y=..` |
| 三屏切换（滑动+按钮） | ✅ | 串口 `DIAG NAV idx=2→1→0` |
| `daily` 定点调度 | ✅ | 串口 `[cron] Loaded 4 cron jobs` |
| 网络状态显示 | ✅ | 串口 `DIAG FILE net=DISCONNECTED`（断开态正确显示） |
| 天气卡片数据 | ✅ | 串口 `DIAG FILE weather=上海 25C 阴` |
| 设备端 Skill 覆盖 | ✅ | 串口 `Skill exists: daily-briefing.md ...` |
| 资产部署 | ✅ 8/8 | `deploy_assets.ps1` 逐字节回读校验全部通过 |
| 圆屏无裁切（离机审计） | ✅ | 几何审计脚本对 10 个元素四角做圆内判定，`FAILURES: 0`，最远角点 220.9 < r=227 |
| 圆屏无裁切（真机） | ✅ | 上板目视确认状态栏/时钟卡/双信息卡/键盘/设置卡/导航胶囊完整，弧形边框无裁切 |
| 运行固件与源码一致 | ✅ | 串口 `DIAG ALIVE 68 zhaoxi_ui.c:839`；行号 839 仅存在于当前 846 行版本（改前 756 行），可证板上固件即本分支构建产物 |

**性能测试**

| 指标 | 实测值 |
|------|--------|
| 固件体积 | 2.33MB（NTC 补丁后 2,445,464 字节） |
| 全量构建 | 2629 步 / 37 分 51 秒（WSL） |
| 增量构建 | 19 步 / 约 8 分钟（圆屏布局改动为单文件重编 + 链接） |
| 中文字体子集 | 262 字 / 4 尺寸（对比全量字体节省数 MB） |
| UI 数据刷新周期 | 2 秒（文件轮询） |
| 圆屏几何审计 | 10 元素 / 40 角点全通过，主机侧 <1 秒（对比上板试错一轮约 13 分钟） |
| LLM 端到端响应时延 | **未量化**。本轮验证聚焦功能正确性与圆屏布局，未按"输入→首字 / 输入→完整回复"两个口径系统计时；定性观察为秒级往返，主要开销在 MiMo 云端推理与 WiFi 链路，端侧 ReAct 的工具调用为本地文件读写、开销可忽略。量化采集已列入演示视频拍摄同步清单（见 3.7 不足项） |
| 内存/资源占用 | **未采集 `ps` 快照**。可量化的间接约束：固件 2.33MB 全量含 4 个 CJK 子集字体（262 字，替代数 MB 全量字体）；`zhaoxi_ui` 与 `ai_agent` 为两个独立任务，UI 侧仅持有 LVGL 上下文与轮询缓冲，无常驻模型。`CONFIG_MQ_MAXMSGSIZE` 由 4096 回调至 64 以消除 `mm_malloc.c:236` 的 DEBUGASSERT 崩溃（消息队列过大挤占堆） |

**可靠性与稳定性测试**

- **心跳检测**：`DIAG ALIVE` 每 30 秒一次，连续运行卡死可检测。本轮真机实测：串口时间戳 `00:34:01` 对应心跳序号 `68`（68×30s ≈ 34min，两者自洽），期间无重启、无堆损坏、无 OOM 类崩溃。**≥24h 长稳测试未做**，列入 3.7 不足项
- **IPC 稳定性**：早期 UI 侧用 socket 向 Agent 查询网络状态，在 LVGL 线程内触发堆损坏；改为文件解耦后该故障模式消除（Agent 写 `NET_STATUS`，UI 只读轮询），且文件缺失时 UI 显示占位而非崩溃
- **断网降级**：天气/新闻不可用时 UI 显示占位不崩溃；对时失败自动回退编译期时间戳
- **存储降级**：/data LittleFS 不可用时自动回退 tmpfs，启动永不阻塞；代价是重启丢资产（已知限制，重跑部署脚本 3 分钟内恢复，见 3.7 改进项）
- **部署可靠性**：921600 高波特率下单比特传输错误 → 回读校验 + 最多 3 轮自动重试，实测 8/8 资产全部一次部署成功

### 3.6 AI-Native 开发说明

| 指标 | 数据 |
|------|------|
| AI Coding 代码占比 | ≈90%（口径：应用层 `zhaoxi_ui`、`tools/` 全部脚本、Skill 资产与文档的净增代码基本由 AI 会话产出；人工承担需求定义、方案裁决、验收与 git 决策；不含组委会模板代码） |
| 使用的 AI 工具 | Codex CLI（前期 10 段会话 / 25,720 条事件）、OpenCode（后期 24 段会话 / 4,187 条事件），共 **34 段会话 / 29,907 条事件**，全部由官方 contest-log-collector 归集至仓内 `logs/` 并通过 `validate-log.py` 校验，可稽核 |
| MCP 工具使用情况 | 未使用；采用等效的"文件所有权收窄 + 禁 git"直接派工模式替代（见下） |
| Skills 使用与新增 | 使用：官方 contest-log-collector。新增：**6 个开发经验 Skill**（`.claude/skills/`：openvela-dev / board-bringup / evidence-driven-debugging / scoped-agent-dispatch / device-skill-authoring / cjk-font-pipeline）+ **4 个设备端运行时 Skill**（`assets/agent_deploy/`） |
| Token 使用总量 | **未统计**。日志侧无法汇总：事件 schema 虽定义了可选字段 `tokens_in`/`tokens_out`（`event.schema.json:89,94`）与会话级 `tokens_total`（`manifest.schema.json:86`），但 Codex 与 OpenCode 适配器实际均未填充（manifest 的 session 记录只有 `event_count`/`health`/`tool` 等，无 `model` 与 token 字段）。设备端 MiMo 推理用量需在小米 MiMo 控制台按账号查询，本项目未按作品单独归集。可稽核的替代量化口径即上行的日志规模（34 段 / 29,907 条事件） |

**AI 工具对开发效率的提升、遇到的问题与解决方式**

- **工作流演进**：从早期"人直接对话式开发"演进为**权限收窄的多智能体派工**——主控先做只读调查（强制 file:line 证据），再按"单文件所有权 + 禁止 git"拆派子任务，子会话改完报告，主控统一 review 后提交。该方法论已沉淀为 `scoped-agent-dispatch` Skill（含任务书模板）。
- **效率证据**：后期 **31 个提交集中在 2 个开发日（9-18 / 9-19）** 内完成，覆盖 UI 数据联动与三屏重写、圆屏安全区布局、两个框架补丁、部署工具链、CJK 字体流水线与全部文档。按可复核口径统计净增行：手写代码/脚本/补丁约 **3,500 行**（`zhaoxi_ui.c` 859 行、`tools/` 四个脚本 973 行、6 个补丁约 900 行、其余为资产与配置），另有 **41,086 行由 `lv_font_conv` 自动生成的字体数组**（4 个尺寸子集）与 11.5MB 会话日志——生成物不计入人工/AI 编码工作量。
- **AI 承担的具体环节**：圆屏布局改造由 AI 完成几何建模（内切圆约束 + 整数开方）并自写离机审计脚本自证；CJK 字体流水线由 AI 从 UI 源码与运行期文案反推所需字集（262 字），并用 char code 构造字面量绕开 PowerShell 5.1 的 ANSI 编码破坏；框架补丁 0006 原为 `CONFIG_AI_AGENT_NET_RPMSG` 未开导致的死代码，由 AI 只读调查定位后重写为 hook `network_is_connected()`。
- **遇到的问题与修正**（全部记录在案）：AI 曾整文件重写导致 CRLF 假 diff（764 行）→ 任务书显式要求保持 LF；AI 默认身份提交导致作者归属错误 → git 权限集中主控；AI 过度承诺（README 写了实际没有的能力）→ 验收逐条核对打回。这些教训全部进入 `openvela-dev` Skill 的 git 纪律章节。

### 3.7 总结与展望

**成果总结**

在 4.5 英寸级圆屏开发板上交付了一个**真机可运行**的主动式 AI 生活管家：4 个定时任务 + 4 个自定义 Skill + 人格记忆三件套构成"主动服务"内核；6 个框架/板级补丁让官方 `ai_agent` 在该平台上真正可用；针对 454×454 圆形 AMOLED 建立了内切圆安全区布局与离机几何审计闸门；2.33MB 固件、11 项功能实测通过；34 段 AI 会话日志（29,907 条事件）与 6 个开发经验 Skill 构成可复用的方法论资产。

**应用前景与商业价值**

- **目标受众**：都市白领（22–40 岁，一二线城市，桌面办公场景为主），重视健康习惯但缺乏自我提醒机制的人群；亦覆盖床头/老人关怀场景（服药提醒等，健康提醒 Skill 可直接复用）。
- **商业模式**：硬件（桌面 AI 管家终端）+ 订阅（个性化简报/关怀服务的云端推理费）；B 端可延伸至企业健康福利采购（久坐提醒 + 员工关怀推送）。
- **规模化潜力**：软件层完全基于开源 openvela + ai_agent，无专有依赖；设备端资产（人格/记忆/Skill）全部为 Markdown 配置，新场景冷启动只需改配置不改代码。

**不足与未来工作**

1. **语音闭环缺失**（当前触摸键盘输入）——下一阶段按三步走：ASR 语音输入（取代触摸键盘，与 cron 主动推送形成"机器主动说、人随口答"的闭环）→ 唤醒词「你好，openvela」+ TTS 播报 → 免手机/免电脑的全语音独立（依赖持久化与供电，见下）。
2. **/data 持久化**：板级分区表暂无 data 分区，当前 tmpfs 回退（重启丢资产，3 分钟重部署恢复）；分区表启用后配置零改动切换（补丁 0002 已预留）。
3. **性能量化未做**：三项指标缺实测数据——LLM 端到端时延（需"输入→首字"与"输入→完整回复"两个口径）、任务内存占用（串口 `ps` 快照）、≥24h 长稳运行。本轮真机验证的连续运行时长为 34 分钟（心跳 68 次无中断）。采集方法已固化进演示视频拍摄清单，可在拍摄同轮补齐。
4. **Token 用量无法自证**：官方日志采集器的 Codex / OpenCode 适配器未填充 `tokens_in`/`tokens_out` 与 `tokens_total`（均为 schema 中的可选字段），导致 AI 开发用量只能以"34 段会话 / 29,907 条事件"这一规模口径稽核。**对上游的改进建议**：适配器补齐 token 字段，或提供从 provider 侧回填用量的工具，否则「Token Plan 追加申请需以日志证明用量」这一流程缺乏可核验的数据支撑。
5. **对话页 LLM 接入**：对话页当前为本地输入回显，完整 LLM 往返走框架消息通道（架构已就绪）。

---

**修订记录**

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-19 | 按官方模板完成 1 信息表 / 2 摘要 / 3.1–3.7 全部章节 |
| v1.1 | 2026-09-20 | 补入圆屏内切圆安全区布局（3.1 难点 6、3.2 关键模块、3.3 机制 6、3.4 交互设计）；固件体积与串口口径按当前构建更正为 2,445,464 字节 / COM3；功能测试由 8 项增至 11 项，新增离机几何审计、真机圆屏目视与"固件—源码一致性"行号证据；心跳可靠性补入实测数据（连续运行 34 分钟 / 68 次心跳）；3.6 补入日志规模量化（29,907 条事件）并说明 Token 用量不可自证的原因与对上游采集器的改进建议 |

*报告版本：v1.1 · 2026-09-20 · 电子农民工*
